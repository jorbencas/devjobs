services:
  twitchrecorder:
    build: .
    container_name: twitchrecorder-sendo
    restart: unless-stopped
    volumes:
      - ./config.json:/app/config.json:ro
      - ../data/grabaciones:/recordings
    environment:
      - TZ=Europe/Madrid
